# Kanna
Built a lightweight GUI wrapper around Pingo to simplify batch image compression for everyday use. The motivation was to reduce large media sizes (e.g., study resources like Anki decks) without noticeable quality loss, while avoiding repetitive command-line usage. 
